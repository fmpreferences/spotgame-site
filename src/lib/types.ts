export interface GameInfo {
    id: string;
    title: string;
    artist_name: string
}

export interface StreamInfo {
    id: string;
    streams: number
}

export interface BtnData {
    id: string;
    title: string;
    artist_names: string
}